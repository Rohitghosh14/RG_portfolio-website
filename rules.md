# RULES.md (v2) — Hard Constraints

Supersedes v1. All v1 rules still apply except where updated below.

## Updated/new rules
1. **No auto-scraping of mockup or stock images.** Device-mockup images must be generated by Rohit using an external mockup tool (upload his own screenshot → tool composites it into a frame) and manually placed in `public/assets/`. No AI model in this project should attempt to download images from mockup/stock sites automatically.
2. **No fabricated "About" photo.** Do not insert a placeholder, stock photo, or generated image into the About bio card. Leave the slot empty/hidden until Rohit provides the real file and confirms which option (real photo / 3D avatar / no photo) per `details.md`.
3. **"Coming Soon" projects (AOD-Net, Dark Pattern Detector) never get a mockup image slot** — they have no live deployment. Their preview gallery relies on the chart component only. Do not fabricate a fake screenshot for these.
4. **Delete unused code, don't leave it dead.** When `ProjectBlock.jsx`, `ProjectPanel.jsx`, and `DeviceMockup.jsx` are replaced in Phase 5, remove them from the codebase entirely rather than leaving unused files behind.
5. **No rush this time.** Per Rohit's explicit instruction, prioritize correctness over speed — confirm each phase step fully working before moving to the next, even across multiple sessions.
6. **If a Coursera cert title can't be confirmed, don't guess it.** The 3 additional verify links in `details.md` v2 don't have confirmed course titles — display them as "Additional verified Coursera credential" with the link, not an invented course name, until Rohit supplies the real title.

## All v1 rules still in force (summary — see v1 `rules.md` for full text)
- Never fabricate a project link or personal info.
- Only one design reference (`moritzpetersen.com` + this system).
- Never restart from scratch on failure — fix/continue existing files.
- Charts are static SVG, no live data fetching.
- One accent color for "live" (navy blue), muted gray for "coming soon" — never mixed.
- No decorative overload (particles, autoplay audio, drop shadows beyond spec, card borders, carousels).
- Cursor stays small, clean, professional — no glow/magnetism.
- Loading screen plays once per session.
- Verify each phase renders correctly before moving on.
- Stay in scope — no blog, backend, CMS, login, analytics.
